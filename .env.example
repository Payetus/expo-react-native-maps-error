# Google Maps SDK for Android key, consumed by app.config.ts at build time.
# Not prefixed with EXPO_PUBLIC_ on purpose: it must stay out of the JS bundle.
# For EAS builds, set the same variable in the build profile's environment.
GOOGLE_MAPS_ANDROID_API_KEY=
